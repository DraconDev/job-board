import JobContainer from "@/components/JobContainer/JobContainer";

export default function Home() {
    return (
        <main className="gap-5 bg-primary w-full overflow-hidden">
            <JobContainer />
        </main>
    );
}
